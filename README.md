# React + Vite
use => npm create vite@latest project-name-- --template react

# Tailwind CSS
Install tailwindcss and @tailwindcss/vite via npm.
Add the @tailwindcss/vite plugin to your Vite configuration.

# Install daisyUI
npm install -D daisyui


